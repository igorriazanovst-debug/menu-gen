<?xml version="1.0" encoding="utf-8"?>
<!-- MG_RELEASE: конфигурация для магазинных сборок — только HTTPS.
     Отладочные адреса (дев-сервер, эмулятор, локальная сеть) вынесены в
     src/debug: раздавать приложение, которое умеет ходить по открытому HTTP,
     нельзя — это и замечание на модерации, и настоящая дыра. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
